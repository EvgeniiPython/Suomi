# Today

## Runtime selection

```text
last_session_type: RETENTION_SESSION
next_session_type: RETENTION_SESSION
continuation_required: NO
```

The 2026-09-17 RETENTION_SESSION is completed. Continuation is not required. The next planned review is 2026-09-20.

## Objective

Развивать самостоятельную профессиональную речь вокруг активных patterns, сохраняя сильный recall и не блокируя acquisition локальными ошибками.

## Priority patterns

1. `saada + object + valmiiksi`
2. `en ehdi + infinitive + partitive object`
3. `sähköpostia / sähköposti / sähköpostin`
4. `sen / sitä` vs `ne / niitä`
5. `työskentelen`
6. `lähetän / lähden`
7. `en soita / soitan`
8. `ennen tapaamista / lounaan jälkeen / tapaamisen jälkeen`

## Next route

The next runtime should use `RETENTION_SESSION` and focus on delayed recall, unlabeled mixed-choice for competing forms, unannounced spelling/recall sampling of `työskentelen`, and changed-context transfer.

## New chunks

Do not introduce a new grammar chunk automatically. Acquisition remains allowed when overall readiness is sufficient; isolated spelling/object-case errors must not automatically block new learning.

## Session record rule

The current session was saved as:

```text
03_Sessions/2026-09-17_Session_Record.md
```

with:

```text
record_schema: canonical-v2
session_type: RETENTION_SESSION
```
