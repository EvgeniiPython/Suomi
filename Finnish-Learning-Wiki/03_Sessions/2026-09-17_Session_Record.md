---
title: Finnish Session Record — 2026-09-17
record_schema: canonical-v2
type: session-record
session_type: RETENTION_SESSION
date: 2026-09-17
tags: [retention, speaking-first, professional-finnish, object-case, transfer]
---

# Finnish Lesson — 2026-09-17

## Session Result
lesson_status: COMPLETED
primary_skill: Delayed retrieval, controlled speaking, changed-context professional dialogue, object-case repair, and spoken transfer

### Evidence
evidence_score: 3
observed: Strong retrieval and transfer across professional contexts. Core patterns were independently produced and corrected through repair. Object-case selection was mostly reliable, while recurring spelling/retrieval slips remained under free-speaking pressure.
- Independently produced `minun täytyy + infinitive`, `saada + object + valmiiksi`, `en ehdi + infinitive + partitive object`, `tapaamisen jälkeen`, `jos en ehdi ...`, `pyydän kollegaani auttamaan minua`, and `soitan / lähetän` patterns.
- Correctly transferred `saada ... valmiiksi` to `piirustukset` and `raportti`; correctly used `raporttia` and `sähköpostia` in unfinished/negative contexts.
- Correctly used `sähköpostin` for a concrete completed object in affirmative context.
- Successfully handled `sen` as a concrete referent in `teen sen` and continued to use plural/singular object contrasts appropriately in dialogue.
- Successfully used `lounaan jälkeen` and `tapaamisen jälkeen` in changed contexts.
- Successfully produced `Jos en ehdi saada projektia valmiiksi, soitan asiakkaalle heti.` independently.
- In Finnish dialogue, repaired `raporttin` → `raportin` and later reproduced `raportin` correctly in a new context.
- Second Chance for `työskentelen` was successful after repeated spelling slips: `työskenteken` / `työskentleen` → `työskentelen`.
- Final recall showed correct `en ehdi lähettää sähköpostia asiakkaalle` and `en ehdi tarkistaa ...`; the remaining spelling slip was `työskente|...` form again, while `pidempään` was correct.
- User clarified that `maali/maalin` was intended to mean a 3D model; recorded as a lexical clarification: `3D-malli`, object `3D-mallin`.

### Protocol Completion
required_stages: retrieval, controlled_speaking, finnish_dialogue, error_repair_second_chance, final_challenge_recall, retention_record
completed_stages: retrieval, controlled_speaking, finnish_dialogue, error_repair_second_chance, final_challenge_recall, retention_record
missing_stages: NONE
continuation_required: NO
continuation_reason: NONE
continuation_next_stage: NONE

### Chunk Decisions
candidate: New chunks
decision: DEFER
reason: Existing priority patterns were broadly functional with successful transfer and repair, but object-case competition and spelling/retrieval slips remain under free speaking. Per acquisition policy, review priority remains high while acquisition is not automatically blocked.
evidence: Strong retrieval, multiple variations, mixed-choice/contrast work, changed-context transfer, Finnish dialogue, second-chance repair, and final speaking challenge completed. No unrelated new grammar chunk introduced.
next_action: Continue delayed monitoring of existing professional patterns. New chunks remain allowed when overall readiness is sufficient rather than being blocked by isolated errors.

### Mastery
current_level: CONSOLIDATING
reason: Core patterns are independently accessible across changed contexts, but some forms are still vulnerable under processing pressure and free recall.
evidence: Correct independent use of `en ehdi + infinitive`, partitive objects, `saada + object + valmiiksi`, `sähköpostia / sähköpostin`, `tapaamisen jälkeen`, `lounaan jälkeen`, `soitan`, and `pyydän kollegaani auttamaan minua`.

### Retention
status: PASSED
next_review: 2026-09-20
evidence: Retrieval, controlled speaking, Finnish dialogue, repair + second chance, final speaking challenge, and final recall completed. Delayed follow-up remains focused on object-case selection and recurring spelling of `työskentelen`.

### Errors
recurring:
- `täytyi` used instead of `täytyy` when the intended meaning was present/current obligation.
- `ennen tapaamisen` → `ennen tapaamista`.
- `ehdi tarkista` → `ehdi tarkistaa` (corrected).
- `jalken/jälken` → `jälkeen` spelling sequence.
- `sähköpostiin` vs `sähköpostin`; context required concrete object `sähköpostin`.
- `jos ..., joten/siten ...` connector competition; repaired to plain conditional clause without extra connector.
- `automaan` → `auttamaan`.
- `raporttin` → `raportin`.
- `valmiineet` → `valmiit`.
- `työskenteken` / `työskentleen` → `työskentelen` — recurring spelling/retrieval slip despite correct `pidempään`.
- `Raportia` → `raportti` when `raportti` is the subject of `ei ole vielä valmis`.
- `velä` → `vielä`.
- `rapotti`/`raporti` type spelling slips remain possible under rapid production.
- lexical clarification: intended `3D-malli`; not `maali`.

correction:
- `Minun täytyy saada raportti valmiiksi ennen tapaamista.`
- `En ehdi tarkistaa piirustuksia tänään.`
- `Tapaamisen jälkeen lähetän sähköpostin asiakkaalle.`
- `Jos en ehdi saada projektia valmiiksi, soitan asiakkaalle heti.`
- `Minun täytyy saada piirustukset valmiiksi ennen tapaamista.`
- `En ehdi saada raporttia valmiiksi ennen tapaamista.`
- `En ehdi lähettää sähköpostia asiakkaalle tänään.`
- `Lähetän sähköpostin asiakkaalle tapaamisen jälkeen.`
- `Jos en ehdi saada piirustuksia ennen tapaamista, pyydän kollegaani auttamaan minua tapaamisen jälkeen.`
- `Jos en ehdi lähettää sähköpostia tänään, soitan asiakkaalle tapaamisen jälkeen.`
- `Raportti ei ole vielä valmis ja en ehdi saada sitä valmiiksi tänään.`
- `Työskentelen tänään pidempään.`
- `Minun täytyy saada 3D-malli valmiiksi ennen tapaamista.`

cause: Object case is increasingly controlled by semantic context, but fast production still produces competition between full object and partitive; spelling/retrieval errors appear during spontaneous speech. `työskentelen` is specifically vulnerable and needs delayed sampling.
next_action: Continue delayed recall with semantic contrasts: `saada + object + valmiiksi` vs `en ehdi ... partitive`, `sähköposti/sähköpostia/sähköpostin`, `sen/sitä`, and quick unannounced recall of `työskentelen / työskentelemään / työskennellä` where context requires it.

### Next Step
next_action: Run the next `RETENTION_SESSION` with priority on `saada + object + valmiiksi`, `en ehdi + infinitive + partitive object`, `sähköposti/sähköpostia/sähköpostin`, `sen/sitä` vs `ne/niitä`, plus delayed recall of `työskentelen` and a smaller check of `lähetän/lähden`, `en soita/soitan`, and time expressions. Include changed-context professional dialogue. New acquisition remains allowed when overall readiness is sufficient.
